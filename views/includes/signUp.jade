div.container.sign-up-form.row(ng-show="data.showSignUp")
	h1 
		| READY,
		br
		| SET, FIGHT 
		br 
		| COLORECTAL CANCER
	form(name="form", class="css-form sign-up-form", ng-submit="addUser()", novalidate)
		input(class="sign-up form-class span", placeholder="Your Name", type="text", id="user-name", ng-model="user.name" name="uName", ng-maxlength="25" required)
		br

		div(ng-show="form.uName.$dirty && form.uName.$invalid" )
			p(ng-show="form.uName.$error.required", class="error-message") Please Enter a Valid Name

		input(class="sign-up form-class span" ,placeholder="Your Email Address", type="email", id="user-email", ng-model="user.email" name="uEmail", required)
		span.why-email(ng-click="showEmailReason()") ?
		br

		div(ng-show="form.uEmail.$dirty && form.uEmail.$invalid" )
			p(ng-show="form.uEmail.$error.required", class="error-message") Please Enter a Valid Email

		input(id="user_id", type="hidden", ng-model="user.id")
		p.ts-and-cs 
			| I accept the game 
			span.terms-link(ng-click="showTerms()") terms and conditions
			| &nbsp; by submitting my informations
		button.btn.btn-primary.span(ng-disabled="form.$invalid") submit 
		//button.btn.btn-primary(ng-click="update(user)", ng-disabled="form.$invalid || isUnchanged(user)") Enter to Play 
